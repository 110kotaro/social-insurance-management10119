# Firebase設定 簡易手順

## 🚀 5分で完了！Firebase設定手順

### 1️⃣ Firebase ConsoleでWebアプリを追加

1. [Firebase Console](https://console.firebase.google.com/) にアクセス
2. プロジェクト **`kensyu10119`** を選択
3. ⚙️（歯車アイコン）→ **「プロジェクトの設定」**
4. 下にスクロール → **「</>（Webアプリを追加）」** をクリック
5. アプリのニックネーム: `social-insurance-management-app`
6. Firebase Hostingのチェックは **外す**
7. **「アプリを登録」** をクリック

### 2️⃣ 設定情報をコピー

表示された設定コードから、以下の値をコピー：
- **必須**: `apiKey`, `authDomain`, `projectId`, `storageBucket`, `messagingSenderId`, `appId`
- **オプション**: `measurementId`（表示されている場合。Firebase Analytics用）

### 3️⃣ environment.tsに貼り付け

1. `src/environments/environment.ts` を開く
2. `YOUR_API_KEY_HERE` などのプレースホルダーを、コピーした値に置き換え
3. `src/environments/environment.prod.ts` にも同じ値を貼り付け

### 4️⃣ 完了！

```bash
npm install @angular/fire firebase
npm start
```

エラーがなければ設定完了です！

---

## 📝 詳細はこちら

詳しい手順やトラブルシューティングは `Firebase設定手順.md` を参照してください。

